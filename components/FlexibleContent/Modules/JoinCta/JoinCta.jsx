import s from './JoinCta.module.sass'

export default function JoinCta ({
    title,
    copy,
    image,
    links
}) {
    return (
        <section className={s.Outer}>
            <h1>
                Join Cta
            </h1>
        </section>
    )
}
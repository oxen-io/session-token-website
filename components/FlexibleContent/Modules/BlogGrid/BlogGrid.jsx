import s from './BlogGrid.module.sass'

export default function BlogGrid ({
    morePostsTitle
}) {
    return (
        <section className={s.Outer}>
            <h1>
                Blog grid
            </h1>
        </section>
    )
}
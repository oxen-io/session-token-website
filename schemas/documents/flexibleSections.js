const components = [
    'hero',
    'iconStrip',
    'joinCta',
    'statsTiles',
    'tileCarousel',
    'copyAndImage',
    'blogGrid',
    'comingSoon',
    'genericCta'
]

const flexibleSections = components.map(component => ({
    type: component
}))

export default flexibleSections

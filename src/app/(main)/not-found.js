import NotFound from "@/components/NotFound/NotFound"

export const metadata = {
    title: "Page not found - Session Token",
    description: "",
}

export default function NotFoundPage({

}) {
    return (
        <NotFound

        />
    )
}
import s from './StatsTiles.module.sass'

export default function StatsTiles ({
    tiles
}) {
    return (
        <section className={s.Outer}>
            <h1>
                Stats tiles
            </h1>
        </section>
    )
}
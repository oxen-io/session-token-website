import s from './TileCarousel.module.sass'

export default function TileCarousel ({
    title,
    content,
    tiles
}) {
    return (
        <section className={s.Outer}>
            <h1>
                Tile carousel
            </h1>
        </section>
    )
}